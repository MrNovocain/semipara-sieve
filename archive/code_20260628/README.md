This directory contains code and notebook snapshots moved out of the active
project paths during the 2026-06-28 cleanup.

These files are retained for historical reference only. They are not current
paper-facing workflows and should not be used as evidence for the main KKT
workbook claims unless they are deliberately reviewed and promoted back into the
active `scripts/`, `configs/`, or `src/pseel/` pipeline.

Archived items:

- `scripts/break_adaptive_results.py`: older mixed Monte Carlo and empirical
  script using the legacy BIC-style partition selector.
- `notebooks/mc_sieve_el*.ipynb`: exploratory notebook snapshots.
